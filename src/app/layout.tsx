import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Areum Jo - Software Engineer",
  description: "Software engineer at Microsoft Azure with 2 years of experience building tools and web applications. Previously a scientist and reporter, based in Seattle, WA.",
  icons: {
    icon: '/favicon.ico',
  },
  openGraph: {
    title: "Areum Jo - Software Engineer",
    description: "Software engineer at Microsoft Azure with 2 years of experience building tools and web applications.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
